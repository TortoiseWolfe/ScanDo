export type ScanTabParams = {
  index: undefined;
  '[id]': { id: string };
};

export type LibraryTabParams = {
  index: undefined;
  '[id]/index': { id: string };
  '[id]/export': { id: string };
};

export type SettingsTabParams = {
  index: undefined;
  subscription: undefined;
};
